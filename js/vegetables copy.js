function runActivity() {
 for (let i = 0; i < song_list; i++) {
  console.log(
    song_list[i] + " - " + song_length[i].charAt(0) + ":" + song_length[i].charAt(1) + song_lengh[i].charAt(2))
  )
 }
}
